#pragma once

#include <bnl/util/export.hpp>

#include <cstddef>
#include <cstdint>

namespace bnl {
namespace util {

BNL_UTIL_EXPORT bool is_lowercase(const char *string, size_t size);

} // namespace util
} // namespace bnl
